<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit a Product</title>
</head>
<body>
    <h1>Edit a Product</h1>

    <form action="http://localhost:3000/products/<%= product._id %>?_method=PUT" method="POST">
        <label for="name">Product name: </label>
        <input type="text" id="name" name="name" value="<%= product.name %>">
        <label for="price">Price (Unit): </label>
        <input type="number" id="price" name="price" value="<%= product.price %>">
        <select name="category" id="category">
            <% for (e of eValues) { %>
            <option value="<%= e%>"  <%= product.category===e ? "selected" : "" %>><%= e.charAt(0).toUpperCase() + e.slice(1)%></option>
            <% } %>
            </select>
        <button>Submit</button>
    </form>
<a href="http://localhost:3000/products/<%= product.id %>">Cancel</a>

</body>
</html>